@model IEnumerable<ViewBag.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create"></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in ViewBag.ProductList) {
    <!--การรับข้อมูลจากคลาส ViewBag ใน View ของไฟล์ส่วนแสดงผล Index.cshtml โดยที่
    1.ข้อมูลสินค้ายังคงอยู่ในรูปแบบเป็นชุด (@model IEnumerable<ViewBag.Models.Product)
    2.วนลูปด้วย foreach อ่านค่าโดยตรงจากคลาส ViewBag (@foreach (var item in ViewBag.ProductList)-->
        <tr>
            <td>
                @item.ID
            </td>
            <td>
                @item.Name
            </td>
            <td>
                @item.Price
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
